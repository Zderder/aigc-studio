# 2026-05-20 工作记录

## 优化 detail.html 加载速度

- 问题：详情页直接调用 `getData()`（会等待云端请求），导致进入时长时间空白
- 解决方案：
  1. **骨架屏（Skeleton Screen）**：页面初始化时立即显示占位动画，避免白屏
  2. **两阶段加载**：Phase 1 用 `getLocalData()` 立即渲染本地数据；Phase 2 后台调用 `getData()` 拉取云端数据，仅当云端数据时间戳更新时才重新渲染
  3. **图片渐显**：主图加 `.img-fadein` + `onload` 回调添加 `.loaded` 类，实现 opacity 0→1 过渡
  4. **缩略图懒加载**：缩略图加 `loading="lazy"` 和 `decoding="async"`
  5. **云端同步浮动提示**：右下角显示"☁️ 正在同步云端数据..."，同步完成后隐藏
- 提取出 `buildDetailHtml(data)` 纯函数，便于两阶段复用渲染逻辑

## 解答 HTTP 404 错误

- 原因：Firebase 数据库 URL 格式填错
- 正确格式：`https://项目名.firebaseio.com`（不是 `.firebaseapp.com` 或 `.web.app`）
- 需要在 Firebase 控制台 Realtime Database 页面找到正确地址
