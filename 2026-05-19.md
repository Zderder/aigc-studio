# 2026-05-19

## AIGC Studio 网站开发日志

### 初始版本
- 构建了完整的 AIGC 作品展示网站（index.html + admin.html）
- 四大板块：AI绘画、AI海报、AI视频、超级字符
- 神经网络粒子背景动画、灯箱查看大图
- 后台管理系统：密码保护、作品上架/下架、数据导出/导入
- localStorage 存储，storage 事件前后台同步

### 视频上传升级
- AI视频板块支持本地视频文件上传（base64 存储）和链接模式切换

### 多图上传升级（本次）
- **admin.html**: 上传区改为多文件选择（multiple），支持拖拽多张图片
- 多图预览网格（80x80 缩略图），悬停显示删除按钮
- 数据结构升级：`item.imgs = [base64...]` 数组，同时保留 `item.img`（第一张）做兼容
- 作品列表增加「N张」角标显示图片数量
- **index.html**: 卡片封面取 imgs[0]，多图时右上角显示图片数量角标
- 灯箱升级：左右箭头翻页 + 底部计数器 + 键盘左右箭头/Esc 导航
- 向后兼容旧数据（只有 img 没有 imgs 的条目）

### 作品详情页
- 新增 **detail.html**：独立的作品详情页，URL 格式 `detail.html?id=xxx&type=painting`
- 从 URL 参数读取 id 和 type，在 localStorage 中查找对应作品数据
- 左侧大图展示区 + 右侧信息面板（标题/工具/日期/描述/点赞）
- 多图支持：左右箭头切换 + 缩略图条 + 底部计数器 + 点击图片全屏查看
- 视频类型：本地视频内嵌播放器，链接类型显示封面+跳转按钮
- 超级字符：大号字符展示
- 键盘导航：左右箭头切图、Esc 返回首页
- 面包屑导航：首页 > 分类 > 作品名
- 找不到作品时显示 404 提示页
- **index.html**: 移除灯箱弹窗（HTML+CSS+JS），卡片点击改为跳转 detail.html
- 点赞功能在详情页中直接操作 localStorage

### Bug修复：作品上传后无法显示（续）
- **根因**: saveData 无 try-catch，localStorage 超限时静默失败；图片未压缩导致 base64 数据过大
- **admin.html 修复**:
  - saveData 添加 try-catch，捕获 QuotaExceededError 并提示用户
  - 保存后二次验证数据是否写入成功
  - 新增图片压缩函数 compressImage()：Canvas 缩放至最大 1600px，JPEG 质量 0.82，自动降质防超限
  - previewMultipleFiles / dropMultipleFiles / previewFile 全部接入压缩
  - 总览面板新增存储空间显示（已用/5MB，颜色随使用率变化）
  - 作品 ID 改为 Date.now()+随机字符串，避免快速连续添加时 ID 重复
- **index.html 修复**:
  - getData 添加日志，便于在控制台排查
  - 本地视频卡片添加 onclick 跳转详情页（原来没有）
  - 新增数据完整性自检 IIFE：检查 localStorage 是否为空、作品是否全下架、上架作品是否缺少图片数据
- **detail.html 修复**:
  - getData 添加日志
  - renderDetail 添加参数诊断日志，未找到作品时输出所有已有 ID

### 视频内嵌播放升级
- **detail.html**: 新增 `getVideoEmbedInfo()` URL 解析函数，根据链接类型自动选择播放方式：
  - 直链 MP4/WebM/MOV → `<video>` 标签页面内直接播放
  - B站链接 (bilibili.com/video/BV...) → iframe 内嵌 B站播放器
  - YouTube 链接 → iframe 内嵌 YouTube 播放器
  - 其他未知链接 → 显示封面图 + 跳转外链按钮（兜底）
- 新增 CSS `.video-embed-container` 用于 iframe 响应式布局
- 信息面板显示视频来源标识（直链视频/哔哩哔哩/YouTube/外部链接）
- **admin.html**: 更新视频链接提示文案，明确标注支持内嵌播放的平台

### IndexedDB 存储迁移（本次）
- **根因**: localStorage 只有 5MB 限制，存几张高清图就满了
- **解决方案**: 新增 `db.js` 共享数据层，从 localStorage 迁移到 IndexedDB
- **db.js**: 封装 openDB/getData/saveData/getStorageEstimate，自动从 localStorage 迁移旧数据
- IndexedDB 存储上限从 5MB 提升到数百 MB~GB（取决于浏览器和磁盘空间）
- **admin.html**:
  - 引入 db.js，移除旧的 localStorage 读写函数
  - addWork/toggleVisible/delete/refreshAll/filterWorks/exportData/handleImport 全部改为 async
  - 存储空间显示改为使用 `navigator.storage.estimate()` API（动态显示已用/总量）
  - 视频上传提示从 "建议 ≤ 50MB" 改为 "空间充足"
- **index.html**:
  - 引入 db.js，移除旧 getData，renderAll 改为 async
  - 跨 tab 同步改为监听 db.js 的 localStorage 通知键
- **detail.html**:
  - 引入 db.js，移除旧 getData，renderDetail/toggleLike 改为 async
  - 点赞操作改用 IndexedDB 的 saveData
- 密码和会话仍使用 localStorage/sessionStorage（数据量极小，无需迁移）

### 云端同步功能（解决跨设备访问问题）
- **根因**: IndexedDB 和 localStorage 都是浏览器本地存储，其他设备打开链接看不到数据
- **解决方案**: 集成 Firebase Realtime Database REST API，数据存储到云端
- **db.js 升级**:
  - 新增 `getEffectiveCloudUrl()`: 优先读 config.json（部署到网站根目录），其次读 localStorage（admin 设备）
  - 新增 `cloudFetchUrl()` / `cloudPushUrl()`: Firebase REST API 读写
  - `getData()` 逻辑：先尝试云端 → 成功则缓存到 IndexedDB → 失败则回退本地
  - `saveData()` 逻辑：先存 IndexedDB（快速可靠）→ 后台异步推送到云端
  - 新增 `testCloudConnection()` / `generateConfigJson()` 辅助函数
- **admin.html 升级**:
  - 设置面板新增「云端同步」区块：Firebase URL 配置、连接测试、启用/关闭同步
  - 总览面板新增云端同步状态指示器
  - 支持下载 config.json 文件（部署到网站根目录即可让所有设备读取云端数据）
  - 详细图文教程引导用户创建 Firebase 项目和配置数据库规则
- **index.html / detail.html**: 无需修改，通过 db.js 统一接口自动支持云端同步
- **跨设备流程**: admin 启用同步 → 下载 config.json → 部署到网站 → 任何设备打开链接自动加载云端数据

### Bug修复：云端同步启用后删除/上传失效
- **根因**: 竞态条件 — `getData()` 优先读云端，`saveData()` 云端推送是 fire-and-forget，admin 中 saveData 调用缺少 await
- **表现**: 删除作品后列表没变，上传新作品后看不到
- **修复** (admin.html):
  - `addWork`/`toggleVisible`/`confirmDelete` 中所有 `saveData()` 加上 `await`
  - `refreshAll(data)` 改为接受可选 data 参数，写操作后直接用内存数据渲染，避免重新从云端读取旧数据
  - 验证步骤从 `getData()` 改为 `getLocalData()`（本地已确保保存完成）

### Bug修复：云端同步启用后添加作品无反应
- **根因**: admin 写操作中 `getData()` 优先从云端读取，Firebase fetch 无超时可能导致按钮点击后挂起
- **修复** (admin.html):
  - `addWork`/`toggleVisible`/`confirmDelete` 中 `getData()` 改为 `getLocalData()`（admin 是数据源头，不需要从云端读）
  - 所有写操作函数加 try-catch，出错时 toast 提示错误信息
- **修复** (db.js):
  - `cloudFetchUrl()` 和 `cloudPushUrl()` 加 AbortController 超时（8秒），防止无限挂起

### 主页板块调整
- AI绘画板块改名为「IP形象」，英文标签改为 IP Character
- IP形象与AI视频板块位置互换，新顺序：AI视频 → AI海报 → IP形象 → 超级字符
- 导航栏、统计栏、Hero 区 CTA 按钮同步调整
