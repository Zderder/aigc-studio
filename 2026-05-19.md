# 2026-05-19

## AIGC Studio 网站开发日志

### 初始版本
- 构建了完整的 AIGC 作品展示网站（index.html + admin.html）
- 四大板块：AI绘画、AI海报、AI视频、超级字符
- 神经网络粒子背景动画、灯箱查看大图
- 后台管理系统：密码保护、作品上架/下架、数据导出/导入
- localStorage 存储，storage 事件前后台同步

### 视频上传升级
- AI视频板块支持本地视频文件上传（base64 存储）和链接模式切换

### 多图上传升级（本次）
- **admin.html**: 上传区改为多文件选择（multiple），支持拖拽多张图片
- 多图预览网格（80x80 缩略图），悬停显示删除按钮
- 数据结构升级：`item.imgs = [base64...]` 数组，同时保留 `item.img`（第一张）做兼容
- 作品列表增加「N张」角标显示图片数量
- **index.html**: 卡片封面取 imgs[0]，多图时右上角显示图片数量角标
- 灯箱升级：左右箭头翻页 + 底部计数器 + 键盘左右箭头/Esc 导航
- 向后兼容旧数据（只有 img 没有 imgs 的条目）

### 作品详情页
- 新增 **detail.html**：独立的作品详情页，URL 格式 `detail.html?id=xxx&type=painting`
- 从 URL 参数读取 id 和 type，在 localStorage 中查找对应作品数据
- 左侧大图展示区 + 右侧信息面板（标题/工具/日期/描述/点赞）
- 多图支持：左右箭头切换 + 缩略图条 + 底部计数器 + 点击图片全屏查看
- 视频类型：本地视频内嵌播放器，链接类型显示封面+跳转按钮
- 超级字符：大号字符展示
- 键盘导航：左右箭头切图、Esc 返回首页
- 面包屑导航：首页 > 分类 > 作品名
- 找不到作品时显示 404 提示页
- **index.html**: 移除灯箱弹窗（HTML+CSS+JS），卡片点击改为跳转 detail.html
- 点赞功能在详情页中直接操作 localStorage

### Bug修复：作品上传后无法显示（续）
- **根因**: saveData 无 try-catch，localStorage 超限时静默失败；图片未压缩导致 base64 数据过大
- **admin.html 修复**:
  - saveData 添加 try-catch，捕获 QuotaExceededError 并提示用户
  - 保存后二次验证数据是否写入成功
  - 新增图片压缩函数 compressImage()：Canvas 缩放至最大 1600px，JPEG 质量 0.82，自动降质防超限
  - previewMultipleFiles / dropMultipleFiles / previewFile 全部接入压缩
  - 总览面板新增存储空间显示（已用/5MB，颜色随使用率变化）
  - 作品 ID 改为 Date.now()+随机字符串，避免快速连续添加时 ID 重复
- **index.html 修复**:
  - getData 添加日志，便于在控制台排查
  - 本地视频卡片添加 onclick 跳转详情页（原来没有）
  - 新增数据完整性自检 IIFE：检查 localStorage 是否为空、作品是否全下架、上架作品是否缺少图片数据
- **detail.html 修复**:
  - getData 添加日志
  - renderDetail 添加参数诊断日志，未找到作品时输出所有已有 ID
