# 2026-05-19 工作日志

## AIGC 作品展示网站

- 创建了完整的 AIGC 作品展示网站，位于 `C:\Users\ABCD\WorkBuddy\2026-05-19-task-2\`
- **index.html** — 前台展示页面：神经网络粒子背景动画、四个展示板块（AI绘画/AI海报/AI视频/超级字符）、灯箱效果、数据统计栏
- **admin.html** — 后台管理系统：密码保护（默认密码 `admin123`）、侧边栏导航、各板块作品上传（支持图片拖拽/预览）、上架/下架 Toggle 开关、删除确认弹窗、数据导出/导入
- 数据存储：使用 `localStorage` 持久化，key 为 `aigc_studio_data`；管理密码 key 为 `aigc_admin_pwd`
- 两页面通过 localStorage storage 事件同步，后台修改实时反映到前台

## 功能升级：视频本地上传支持

- admin.html AI视频板块新增「上传本地视频/填写视频链接」双模式切换
- 本地视频（mp4/webm/mov）以 base64 DataURL 形式存储到 localStorage，字段名 `videoData`
- 后台上传区域支持拖拽视频 + 在线预览
- index.html 前台：有 videoData 时直接内嵌 `<video controls>` 播放器，URL 模式保持原跳转逻辑
- 作品列表增加「本地/链接」来源标签区分
